<nav
  class="navbar navbar-expand-lg navbar-light bg-light fixed-top border-bottom shadow-sm"
>
  <a class="navbar-brand font-weight-bold" href="/projects"
    ><i class="fas fa-hands-helping"></i> Crowdfunding</a
  >
  <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarNav"
    aria-controls="navbarNav"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse font-weight-bold" id="navbarNav">
    <div class="navbar-nav ">
      <a class="nav-link <%= currentUrl === '/projects' ? 'active' : '' %>" href="/projects">Home</a>
      <a class="nav-link" href="/projects#burgeoning-projects"
        >Funded Projects</a
      >
      <a class="nav-link <%= currentUrl === '/projects/new' ? 'active' : '' %>" href="/projects/new">Start a Project</a>
    </div>
    <div class="navbar-nav ml-auto">
      <% if (!currentUser) { %>
      <a class="nav-link <%= currentUrl === '/login' ? 'active' : '' %>" href="/login"
        ><i class="fas fa-sign-in-alt"></i> Login</a
      >
      <a class="nav-link <%= currentUrl === '/register' ? 'active' : '' %>" href="/register"
        ><i class="fas fa-user-plus"></i> Register</a
      >
      <% } else { %>
      <a class="nav-link" href="<%= currentUser.isAdmin ? "/admin" : "#" %>"
        ><i class="fas fa-user-alt"></i> <%= currentUser.isAdmin ? "Admin Dashboard" : currentUser.username  %>
      </a>
      <a class="nav-link" href="/logout"
        ><i class="fas fa-sign-out-alt"></i> Logout</a
      >
      <% } %>
    </div>
  </div>
</nav>
